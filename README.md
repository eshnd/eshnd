```c++ but actually c
void eshnd(){
  char* interests[] = {"coding", "cartoons", "games"};
  int* coolness = NULL;
  int happiness = random(INT_MIN, INT_MAX);
  char* pfp_source = "Adventure Time - Cartoon Network";
}

```
